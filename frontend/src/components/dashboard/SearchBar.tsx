import { TextField } from "@mui/material";

export default function SearchBar() {
  return (
    <TextField
      fullWidth
      label="Search Production Lines"
      variant="outlined"
      size="small"
    />
  );
}