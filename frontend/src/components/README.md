# Frontend Components

Reusable UI lives here.

## Current Groups

- `common/`: shared small components such as chips and general-purpose widgets
- `dashboard/`: dashboard-specific visualization components
- `layout/`: navigation, shell, and surrounding UI structure

If a component is only used by one page, consider keeping it near that page until reuse is clear.