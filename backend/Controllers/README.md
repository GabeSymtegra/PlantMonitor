# Backend Controllers

Controllers expose HTTP endpoints that are better expressed as grouped controller classes instead of inline mappings.

## Current Contents

- `LinesController.cs`: line-oriented endpoints
- `Admin/`: administration APIs, especially PLC and configuration workflows

Minimal API mappings still exist in `Program.cs` for core runtime and auth endpoints.